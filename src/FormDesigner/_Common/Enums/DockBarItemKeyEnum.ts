export enum DockBarItemKeyEnum {
    // 组件面板
    COMPONENT = 'component',

    // 表单Schema结构
    SCHEMA_STRUCTURE = 'schema_structure',

    // 表单Schema代码
    SCHEMA_CODE = 'schema_code',

    // 数据源配置
    DATASOURCE_CONFIG = 'datasource_config',

    // 操作历史记录
    OPERATE_HISTORY = 'operate_history',

}